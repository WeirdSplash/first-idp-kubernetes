# Backstage

This folder will contain the Backstage installation and custom software templates.

Instructions for setup will be added as the demo is built.

## Quick reference

- `app-config.yaml` — Backstage main configuration
- `templates/` — custom software templates
